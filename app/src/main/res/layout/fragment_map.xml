<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/settings_background">

    <org.osmdroid.views.MapView
        android:id="@+id/map_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Floating attribution chip (Kartverket / NVE / Regobs are open data but require credit). -->
    <TextView
        android:id="@+id/map_attribution"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_margin="8dp"
        android:background="#A0000000"
        android:paddingHorizontal="8dp"
        android:paddingVertical="4dp"
        android:text="© Kartverket • Bratthet © NVE • Obs. © Regobs"
        android:textColor="@color/settings_text_primary"
        android:textSize="10sp" />

    <!-- Center-on-me button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/center_on_me_button"
        style="@style/Widget.MaterialComponents.Button.Icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:contentDescription="Sentrer på min posisjon"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        app:backgroundTint="@color/settings_surface"
        app:cornerRadius="24dp"
        app:elevation="6dp"
        app:icon="@android:drawable/ic_menu_mylocation"
        app:iconGravity="textStart"
        app:iconPadding="0dp"
        app:iconTint="@color/settings_accent"
        app:iconSize="24dp" />

    <ProgressBar
        android:id="@+id/map_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="16dp"
        android:indeterminateTint="@color/settings_accent"
        android:visibility="gone" />
</FrameLayout>
